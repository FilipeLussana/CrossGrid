Context:
- Repo root: c:/Users/FL-Test-PC/Documents/VS_CODE/CrossGrid

Task:
Create a short manual test plan and a few automated sanity checks (as JS snippets) to validate core behaviors:
- placing pieces, moving, captures, blocked nodes expiring, AI moves, victory detection.

Deliverables:
- `manual_tests.md` describing steps to reproduce each core scenario.
- `sanity_tests.js` (not a full test harness) with functions that can be run in browser console to assert key invariants.
